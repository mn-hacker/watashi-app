name: proxy_url_parser
description: Proxy Url Parser helps to convert vmess/vless proxy urls to json format
version: 1.0.0

environment:
  sdk: ^3.7.0

dependencies:
  equatable: ^2.0.7

dev_dependencies:
  lints: ^5.1.1
  test: ^1.26.2
